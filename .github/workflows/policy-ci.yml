name: policy-ci
on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Portfolio mode: skipping cloud login; CI passes by design."